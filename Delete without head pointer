class Solution
{
    void deleteNode(Node del)
    {
         // Your code here
         if(del.next==null){
             return;
         }
         Node node=del.next;
         del.data=node.data;
         del.next=node.next;
    }
}

